# Snowizard-specific options.
snowizard:

  datacenterId: 1
  workerId: 1

# Riak-specific options.
riak:

  nodes:
    - 127.0.0.1:8087

# HTTP-specific options.
server:

  applicationConnectors:
    - type: http
      port: 8080
  
  adminConnectors:
    - type: http
      port: 8180

  requestLog:
    appenders:
      - type: console
        timeZone: UTC
        target: stdout
#     - type: file
#       currentLogFilename: /var/log/notification/requests.log
#       threshold: ALL
#       archive: true
#       archivedLogFilenamePattern: /var/log/notification/requests-%d.log
#       archivedFileCount: 5
#       timeZone: UTC

logging:
  level: INFO
  loggers:
    com.smoketurner.notification: DEBUG
    com.basho.riak: DEBUG
  appenders:
    - type: console
      timeZone: UTC
      target: stdout
#    - type: file
#      currentLogFilename: /var/log/notification/app.log
#      threshold: ALL
#      archive: true
#      archivedLogFilenamePattern: /var/log/notification/app-%d.log
#      archivedFileCount: 5
#      timeZone: UTC
